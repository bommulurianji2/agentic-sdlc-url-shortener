# Shell scripts must always check out with LF, or the shebang breaks when
# Docker copies them into the Linux container - this matters specifically
# for a Windows reviewer with core.autocrlf=true (a common default), whose
# checkout would otherwise silently corrupt docker-entrypoint.sh.
*.sh text eol=lf
